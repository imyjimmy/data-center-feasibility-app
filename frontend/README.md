# Frontend

React + Vite app for the data center feasibility MVP.

## Commands

```sh
npm install
npm run dev
npm run build
npm run preview
```

Set `VITE_API_BASE_URL` to point the app at a non-local backend.

